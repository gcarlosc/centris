<!-- <div id="myModal" class="modal fade in">
  <div class="modal-dialog">
    <div class="modal-content">
       <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
          <h4 class="modal-title">Crear Skus</h4>
        </div>
      <div class="modal-body">
        <div class="size-adaptable border-form">
<<<<<<< HEAD:app/views/line_items/_form_edit.html.erb
          <div class="panel-body">
=======
          <div class="panel-body"> -->
            <table class="table show-message">
              <thead>
                <tr>
                  <th>Producto</th>
                  <th>SKU</th>
                  <th>*</th>
                </tr>
              </thead>
              <% if @line_item.movement.movement_type_id == 1 %>
                <% @line_item.skus.each do |sku| %>
                  <%= render partial: 'skus/form_externo', locals: {line_item: @line_item, sku: sku} unless sku.new_record? %>
                <% end %>
              <% else %>
                <% @line_item.quantity.times do %>
                  <%= render 'skus/form_interno' %>
                <% end %>
              <% end %>
            </table>
          <!-- </div>
        </div>
      </div>
    </div>
  </div>
</div> -->
