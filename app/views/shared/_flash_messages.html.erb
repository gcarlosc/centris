<% flash.each do |message_type, message| %>
  <div id="flash" class="alert <%= bootstrap_class_for(message_type) %> fade in">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>

